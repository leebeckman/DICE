package aspects;

import java.util.HashMap;

import javax.servlet.ServletRequest;

public class RequestData {

//	private static HashMap<Long, ServletRequest> threadRequestMap = new HashMap<Long, ServletRequest>();
//	
//	public static void mapCurrentThreadToRequest(ServletRequest req) {
//		threadRequestMap.put(Thread.currentThread().getId(), req);
//	}
//	
//	public static int getCurrentThreadRequestHash() {
//		if (threadRequestMap.containsKey(Thread.currentThread().getId()))
//			return threadRequestMap.get(Thread.currentThread().getId()).hashCode();
//		else
//			return 0;
//	}
	
}
